# This is a TEMPLATE — do not put real credentials in a file that goes to GitHub.
#
# bootstrap_cml.sh generates the real .streamlit/secrets.toml automatically
# at runtime, pointing at the Postgres instance it starts inside your CML
# session. You should never need to edit this by hand — it's here so the
# shape is documented, and as a fallback if you ever need to point the app
# at a different Postgres instance manually.

DATABASE_URL = "postgresql://appuser:REPLACE_ME@localhost:5432/dataproducts"
